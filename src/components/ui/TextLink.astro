---
interface Props {
  href: string;
  class?: string;
}
const { href, class: className = '' } = Astro.props;
---

<a
  href={href}
  class={`type-button inline-flex items-center gap-1.5 text-brand-navy transition-colors hover:text-brand-blue-bg ${className}`}
>
  <slot />
</a>
