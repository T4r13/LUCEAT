---
interface Props {
  as?: 'p' | 'span';
  class?: string;
}
const { as: Tag = 'p', class: className = '' } = Astro.props;
---

<Tag class={`text-sm font-semibold uppercase tracking-[0.22em] text-brand-blue ${className}`}>
  <slot />
</Tag>
